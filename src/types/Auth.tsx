export enum Auth {
    LOGIN,
    LOGOUT
}

export enum Role {
    GUEST,
    USER
}

export enum Enum_AuthStatus {
    Login,
    Register,
    Find
}

export enum Enum_RegisterProgress {
    EMAIL,
    ID,
    PW,
    SUCCESS
}